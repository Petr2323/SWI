function safeProcessAndDisplay(input) {
  // Create a new div element
  const div = document.createElement('div');

  // Use textContent to automatically escape any HTML or scripts
  div.textContent = input;

  // Optionally, you can add some styling or attributes here

  // Append the safely escaped content to the body
  document.body.appendChild(div);
}
